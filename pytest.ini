[pytest]
# Disable pytest-qt plugin to avoid requiring a functional PySide6 QtGui in headless CI.
addopts = -p no:pytestqt
env =
    NUMBA_DISABLE_JIT=1
markers =
    pets_model_contract: real official YOLOX model and image contract
    pets_scale_contract: large synthetic Pets incremental-index contract
